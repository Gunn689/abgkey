{
  "name": "abgkey",
  "version": "1.0.0",
  "private": true,
  "description": "ABGunnn license + feedback server",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  },
  "engines": { "node": ">=18" }
}
